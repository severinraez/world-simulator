'use strict';

let roles = (spec) => {
    return spec
}

let begin = (work, flow, roles, devices) => {
    // Initialize devices.

    // Initialize local device (if needed).
}

let cooperate = (work) => {
    // Await configuration

    // Initialize local device
}

module.exports = {
    roles: roles
}
